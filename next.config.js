/** @type {import('next').NextConfig} */
const nextConfig = {
  // Dica: para expor o servidor de desenvolvimento na rede local
  // execute: `next dev -H 0.0.0.0` (ou ajuste o script "dev").
  // Não há opção experimental "allowedDevOrigins" nas versões atuais do Next.js.
};

export default nextConfig;
